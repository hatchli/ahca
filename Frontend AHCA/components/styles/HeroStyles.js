import styled from "styled-components";

const HeroStyles = styled.div`
  margin: 0;
  padding: 0;
  display: flex;
  justify-self: end;
  font-size: 2rem;
  max-width: 100%;
  img {
    width: 100%;
    height: 60%;
    object-fit: cover;
  }
`;

export default HeroStyles;
