import { ProviderAliases } from '@prisma/sdk';
export interface StudioOptions {
    projectDir?: string;
    port?: number;
}
export declare class Studio {
    private projectDir;
    private instance?;
    private port?;
    constructor({ projectDir, port }?: StudioOptions);
    start(providerAliases: ProviderAliases): Promise<string>;
    restart(providerAliases: ProviderAliases): Promise<string>;
    private getSchemaDir;
}
