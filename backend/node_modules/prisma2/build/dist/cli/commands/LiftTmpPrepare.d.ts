import { Command, HelpError } from '@prisma/cli';
/**
 * $ prisma migrate new
 */
export declare class LiftTmpPrepare implements Command {
    static new(): LiftTmpPrepare;
    private static help;
    private constructor();
    parse(argv: string[]): Promise<string | Error>;
    help(error?: string): string | HelpError;
}
