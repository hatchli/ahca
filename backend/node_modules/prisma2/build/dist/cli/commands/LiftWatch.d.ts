import { Command, HelpError } from '@prisma/cli';
import { ProviderAliases } from '@prisma/sdk';
/**
 * $ prisma migrate new
 */
export declare class LiftWatch implements Command {
    private readonly providerAliases;
    static new(providerAliases: ProviderAliases): LiftWatch;
    private static help;
    private constructor();
    parse(argv: string[]): Promise<string | Error>;
    help(error?: string): string | HelpError;
}
