import { GeneratorOptions } from '@prisma/cli';
export declare type GeneratorWorkerJob = {
    packagePath: string;
    config: GeneratorOptions;
};
export declare function generateInThread(options: GeneratorWorkerJob): Promise<string>;
