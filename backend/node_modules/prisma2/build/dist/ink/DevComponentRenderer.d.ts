import { Instance } from 'ink';
import { EngineResults } from '../types';
import { DevComponentProps, GeneratorInfo } from './DevInkComponent';
export interface DevComponentOptions {
    port: number;
    initialState: DevComponentProps;
}
export declare class DevComponentRenderer {
    state: DevComponentProps;
    app?: Instance;
    count: number;
    lastNewState?: any;
    private warningsPromptCallback?;
    constructor(options: DevComponentOptions);
    setGeneratorState(index: number, gen: Partial<GeneratorInfo>): void;
    setState(state: Partial<DevComponentProps>): void;
    promptForWarnings(warnings: EngineResults.Warning[]): Promise<boolean>;
    render(): Instance;
}
