import { LockFile } from '../types';
export declare function deserializeLockFile(file: string): LockFile;
export declare function serializeLockFile(lockFile: LockFile): string;
export declare function initLockFile(): LockFile;
